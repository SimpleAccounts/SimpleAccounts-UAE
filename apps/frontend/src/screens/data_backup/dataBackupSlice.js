import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const dataBackupSlice = createSlice({
  name: 'data_backup',
  initialState,
  reducers: {},
});

export default dataBackupSlice.reducer;

